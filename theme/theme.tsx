import { StyleSheet } from "react-native";

//creacion del estilo global que se usará en todo el project
export const styles = StyleSheet.create({

    container: {
       marginHorizontal: 20,
    },
});